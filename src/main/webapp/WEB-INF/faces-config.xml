<?xml version="1.0" encoding="UTF-8"?>
<faces-config xmlns="https://jakarta.ee/xml/ns/jakartaee"
              xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-facesconfig_4_0.xsd"
              version="4.0">

  <!--
    JSF ライフサイクル内で発生した例外を一括ハンドリングするカスタム ExceptionHandler を登録する。
    これにより MSTBusinessException（回復可）は現画面に FacesMessage として表示され、
    MSTBusinessNonRecoverException（回復不可）や想定外例外は error.xhtml へ遷移する。
    本登録が無いと JSF は標準ハンドラを使い、業務エラーも web.xml の Throwable error-page 経由で
    error.xhtml に全画面遷移してしまう（技術設計書 §4 のエラーハンドリング方針参照）。
  -->
  <factory>
    <exception-handler-factory>
      jp.mufg.it.rcb.exception.handler.FacesExceptionHandlerFactory
    </exception-handler-factory>
  </factory>

  <application>
    <locale-config>
      <default-locale>ja</default-locale>
      <supported-locale>ja</supported-locale>
    </locale-config>
    <resource-bundle>
      <base-name>message</base-name>
      <var>msg</var>
    </resource-bundle>

    <!--
      ExceptionFacesResponseHandler が HTTP ステータス別のリダイレクト先を解決するためのエントリ。
      handler は getElementsByTagName("entry") で key=error.page.{status} / error.page.default を走査する。
      回復不可の業務エラー(500)・想定外例外(500) はここで /error.xhtml に着地する。
    -->
    <entry>
      <key>error.page.500</key>
      <value>/error.xhtml</value>
    </entry>
    <entry>
      <key>error.page.default</key>
      <value>/error.xhtml</value>
    </entry>
  </application>

</faces-config>
