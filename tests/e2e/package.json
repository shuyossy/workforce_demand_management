{
  "name": "sak-dev-env-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
