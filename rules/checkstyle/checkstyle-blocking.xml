<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
  "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
  "https://checkstyle.org/dtds/configuration_1_3.dtd">
<!--
    Checkstyle hard gate: プロジェクト独自の致命パターン集合（severity="error"）。
    pre-commit（差分対象）と pre-push / CI（全体対象）の両方で実行する。

    収録方針: 誤検知が実質ゼロで修正方法が自明なルールのみ。Spotless が整形で担保する
    範囲（インデント / import 並び / Whitespace 等）は原則含めない。例外として
    NewlineAtEndOfFile / 行末空白 / Tab は hooks バイパス時の保険として残す。

    詳細は rules/README.md §4.1 参照。
-->
<module name="Checker">
  <property name="charset" value="UTF-8"/>
  <property name="severity" value="error"/>
  <property name="fileExtensions" value="java"/>

  <!-- 行末空白: Spotless の trimTrailingWhitespace と重複するが、hooks バイパス (no-verify) 時の保険として残す -->
  <module name="RegexpSingleline">
    <property name="format" value="\s+$"/>
    <property name="message" value="行末に空白があります"/>
  </module>

  <!-- Tab 文字: Checkstyle ネイティブの FileTabCharacter で検知 (eachLine=true で全行報告) -->
  <module name="FileTabCharacter">
    <property name="eachLine" value="true"/>
  </module>

  <!-- EOF 改行: Spotless の endWithNewline と重複するが、hooks バイパス (no-verify) 時の保険として残す -->
  <module name="NewlineAtEndOfFile"/>

  <module name="TreeWalker">
    <module name="UnusedImports"/>
    <module name="RedundantImport"/>
    <module name="AvoidStarImport"/>

    <!-- 空 catch: advisory (google_checks) と揃え、意図的 no-op を示す変数名 "expected" は許容 -->
    <module name="EmptyCatchBlock">
      <property name="exceptionVariableName" value="expected"/>
    </module>

    <module name="MissingOverride"/>
    <module name="EqualsHashCode"/>
    <module name="SimplifyBooleanExpression"/>
    <module name="StringLiteralEquality"/>
    <module name="ModifierOrder"/>
  </module>
</module>
